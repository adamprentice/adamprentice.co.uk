import { Metadata } from "next";
import "./page.scss";

export const metadata: Metadata = {
  title: "Resume - Adam Prentice",
  description: "My Resume",
};

export default function Home() {
  return <main className=""></main>;
}
